#
# "main" pseudo-component makefile.
#
# (Uses default behaviour of compiling all source files in directory, adding 'include' to include path.)

COMPONENT_ADD_INCLUDEDIRS += loop
COMPONENT_SRCDIRS += loop

COMPONENT_ADD_INCLUDEDIRS += view
COMPONENT_SRCDIRS += view

COMPONENT_ADD_INCLUDEDIRS += model
COMPONENT_SRCDIRS += model

COMPONENT_ADD_INCLUDEDIRS += driver
COMPONENT_SRCDIRS += driver

COMPONENT_ADD_INCLUDEDIRS += widget
COMPONENT_SRCDIRS += widget

